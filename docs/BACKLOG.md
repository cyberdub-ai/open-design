# BACKLOG — форк open-design

Заведён 05.09.2026 автономным прогоном. Порядок = порядок работы: беру сверху.

Критерий попадания сюда: поведение, которое форк добавил или изменил поверх апстрима,
и которое ничем не удерживается. Апстримный мерж такое молча откатывает — ровно это
уже случилось дважды (`b93beada73` вернул `stdinOpen`, `e733c1118f` вернул `update_project`).

## A. Регрессии: поведение форка исчезло из дерева

1. **`OD_MCP_READONLY` / `WRITES_ENABLED` (из `e58fc262cf`) отсутствует полностью.**
   Все пишущие MCP-тулы регистрируются безусловно (`apps/daemon/src/mcp.ts`, `TOOL_DEFS`).
   Намерение оператора «только чтение» сейчас нечем выразить. Чинить с красным тестом:
   при `OD_MCP_READONLY=1` `localMcpToolDefinitions()` не содержит пишущих тулов.
2. ~~**`OD_EXTRA_ORIGINS`**~~ — ЗАКРЫТО 05.09, кода не требует. Форковый коммит `78cdef47d1`
   (а не `944fe8756e`, как значилось в инвентаризации) просто подмешивал строки в набор
   разрешённых origin. Апстримный `OD_ALLOWED_ORIGINS` делает то же строже: разбирает URL,
   отбивает не-http(s), нормализует origin (`apps/daemon/src/origin-validation.ts`). Это
   вытеснение, а не потеря, и прод на него уже переехал:
   `open-design.service` несёт `OD_ALLOWED_ORIGINS=https://open-design.cyberdub.su`.
   Возвращать форковое имя незачем — оно было бы вторым способом сказать то же самое.
3. ~~**`--skip-discovery` в CLI никогда не был реализован**~~ — ЗАКРЫТО 05.09, коммит
   `c763b2b93e`. Заявку сделал форковый коммит `78cdef47d1` (в инвентаризации стоял
   неверный `944fe8756e`), но `cli.ts` он не трогал вовсе. Реализовано: флаг зарегистрирован
   в `PROJECT_BOOLEAN_FLAGS` (без этого `parseFlags` отбивает его как неизвестный),
   описан в help и кладёт `skipDiscoveryBrief: true` в тело `POST /api/projects` только
   когда его попросили. Веб-поверхности нет намеренно — это то самое исключение из правила
   двойной поверхности: UI собирает бриф интерактивно, а флаг существует ровно для
   автоматических прогонов, где отвечать некому.
   Проверено так: `pnpm exec vitest run tests/cli-project-create-discovery.test.ts` → 3 passed
   (2 из 3 были красными до правки).

## B. Живое поведение форка без единого теста

4. ~~`stdinOpen=true`~~ — ЗАКРЫТО 05.09, коммит `7192bdeac4`. Утверждать константу
   `run.stdinOpen` было бы бесполезно: мерж, переразрешивший ветку, оставил бы тест зелёным.
   Поэтому обе ветки записи промпта вынесены в именованную
   `writeComposedPromptToChildStdin` (`apps/daemon/src/runtimes/chat-run-lifecycle.ts`),
   а тесты утверждают наблюдаемый эффект: какие байты ушли в stdin и был ли вызван `end()`.
   Поведение не менялось — JSONL-конверт, глотание EPIPE и учёт backpressure переехали как есть.
   Проверено так: `pnpm exec vitest run tests/chat-run-stdin-prompt.test.ts` → 7 passed
   (все 7 красные до появления хелпера).
5. ~~Тул `update_project`~~ — ЗАКРЫТО 05.09, коммит `bb103125b1`. Четыре случая на границе
   тула: в тело PATCH попадают только переданные поля; пустой патч отбивается без запроса;
   переименование сбрасывает 5-секундный кэш списка проектов; не-переименование его хранит.
   Проверено так: `pnpm exec vitest run tests/mcp-update-project.test.ts` → 4 passed; снятие
   строки `projectListCache = null` из `mcp.ts` красит ровно тест про переименование
   (мутационная проверка), после чего `mcp.ts` восстановлен байт-в-байт.
6. ~~Локальный Ollama по умолчанию~~ — ЗАКРЫТО 05.09, коммит `2493a2a779`. `OLLAMA_HOST`
   читается на загрузке модуля, поэтому тест грузит модули заново через `vi.resetModules()`
   и утверждает URL, по которому `extractWithLLM` реально ходит.
   Проверено так: `pnpm exec vitest run tests/memory-ollama-host.test.ts` → 2 passed.
7. ~~Сторож простоя codex 30 мин~~ — ЗАКРЫТО 05.09, коммит `51bad8e521`. Утверждается и поле
   `codexAgentDef.inactivityTimeoutMs`, и значение, которое отдаёт
   `resolveChatRunInactivityTimeoutMs` — тот же резолвер, что на боевом пути.
   Проверено так: `pnpm exec vitest run tests/runtimes/chat-run-inactivity-timeout.test.ts` → 32 passed.
8. ~~`OD_CLAUDE_STRICT_MCP`~~ — ЗАКРЫТО 05.09, коммит `2ddb2bb9dc`. Флаг появляется только при
   значении ровно `'1'`, отсутствует по умолчанию и при `'0'`/`'true'`/`'yes'`/`''`, и стоит
   раньше `--permission-mode`.
   Проверено так: `pnpm exec vitest run tests/runtimes/claude-strict-mcp-args.test.ts` → 4 passed.
9. ~~Выпадашка выбора скилла~~ — ЗАКРЫТО 05.09, коммит `192ce72d43`. Пять тестов на
   наблюдаемом контракте: какие опции даёт выпадашка на каждой вкладке, какой `skillId`
   доезжает до `onCreate` при «— авто —» и при явном выборе, что выбор не протекает между
   вкладками и что на вкладках без маршрутизации через скилл выпадашки нет.
   Проверено так: `pnpm --filter @open-design/web exec vitest run tests/components/NewProjectPanel.skill-picker.test.tsx`
   → 5 passed; мутационная проверка двумя правками ПО ОТДЕЛЬНОСТИ (снятие `selectedSkillId ??`
   из payload и снятие `useEffect` сброса на смене вкладки) — вместе они маскируют друг друга
   и красят только один тест; `pnpm --filter @open-design/web typecheck` → 0 `error TS`.

## B'. Найдено по ходу, не начато

10. ~~**[РАЗВИЛКА] Захардкоженный русский в UI.**~~ — ЗАКРЫТО 05.09, коммит `fb24d8bfeb`.
    Проверено так: восстановление литерала `Скилл` красит 4 теста из 5 (мутационная проверка),
    `pnpm --filter @open-design/web typecheck` → `WEB_TC=0`, 0 `error TS` (гейт на все 19 локалей).
    Исходная формулировка: `apps/web/src/components/NewProjectPanel.tsx:970`
    (`Скилл`) и `:978` (`— авто —`) — единственные нелокализованные строки интерфейса
    (полный скан кириллицы по `apps/web/src`: все остальные попадания — локализованные
    примеры промптов в `HomeHero.tsx`). Каждый неруссоязычный пользователь видит там кириллицу.
    Решение: отдельный коммит — ключи `newproj.skillLabel` / `newproj.skillAuto` в
    `i18n/types.ts` и во все 19 локалей, затем матчер в тесте с `/скилл/i` на английскую
    подпись. Скрипт правки готов: `scratchpad/i18n_skill_keys.py` (переводы для всех 19
    локалей вшиты, определяет стиль кавычек по файлу). Не начато сознательно: сессию
    закрывают, крупных задач больше не начинаю.

11. ~~**Локаль-условная ссылка на комьюнити не защищена тестом.**~~ — ЗАКРЫТО 05.09, коммит
    `71e208ed68`. Четыре спека в `apps/web/tests/components/EntryNavRail.community-locale.test.tsx`
    утверждают href, `data-testid` и `track()` для `zh-CN`, `zh-TW`, `en`, `ja`. Приёмка
    мутационная, по одной мутации за раз: снятие терма `|| locale === 'zh-TW'` → 1 failed |
    3 passed; схлопывание условия в `false` → 2 failed | 2 passed; восстановление →
    4 passed при пустом `git diff --stat` на компоненте.
    Исходная постановка: форк отдаёт китайским
    локалям инвайт в Feishu вместо Discord: `apps/web/src/components/EntryNavRail.tsx:1147`
    (`const isChinese = locale === 'zh-CN' || locale === 'zh-TW';`), используется на 1149/1168/
    1169/1171 — aria-label, `data-testid`, `track()`, иконка. Тестов нет вовсе: ни один файл в
    `apps/web/tests/**` не упоминает `Feishu`/`Discord`, включая `EntryNavRail.analytics.test.tsx`.
    Почему это опасно: поведение — один терм булева выражения внутри `EntryNavRail`, который
    апстрим активно правит (`#7237`, `#7202`, `#7190`). Мерж, переписавший соседний соцблок,
    разрешится без конфликта, если уберёт условие или схлопнет его в `false`; typecheck это
    пропустит, красить нечему. Тест должен утверждать наблюдаемое: при `locale='zh-CN'` и
    `'zh-TW'` в DOM ссылка на `feishu.cn` с `data-testid="entry-nav-rail-feishu"`, клик шлёт
    `track('feishu', …)`; при любой другой локали — `discord.gg` и `track('discord', …)`.
    Проверять именно ссылку, которую видит и кликает пользователь, а не наличие констант
    `DISCORD_URL`/`FEISHU_URL`. Приёмка — мутационная: убрать `|| locale === 'zh-TW'` и увидеть
    красный. Риск высокий, это единственная незащищённая форк-правка вне `apps/daemon`.

12. **Порядок карточки входа в футере nav rail не защищён (риск низкий).** Коммит `8934000924`
    заменил `position: absolute; bottom: 3px; z-index: 2` на `order: 2` для
    `.entry-local-mode-tip` в `apps/web/src/styles/home/entry-layout.css` (~421–520), чтобы
    непрозрачная карточка не перекрывала соцссылки (`order: 3`). CSS-мерж разрешается построчно
    и структурного якоря тут нет. Существующий `apps/web/tests/styles/cloud-signin-tip-selectable-text.test.ts`
    проверяет только выделяемость текста, не порядок. Тест должен утверждать эффект, а не
    значение свойства: у неавторизованного пользователя одновременно видимы и кликабельны и
    карточка входа, и соцссылки футера. Если это дорого выразить без реального лейаута — честнее
    завести как пункт для Playwright, чем писать проверку строки CSS.

13. **`terminals.spawn-helper.test.ts` не гоняется на Linux — молча.** Гейт:
    `const describeIfHelper = helperPath ? describe : describe.skip;`, где `helperPath` ищет
    `spawn-helper` в `<node-pty>/build/Release/` или `<node-pty>/prebuilds/<platform>-<arch>/`.
    На этом хосте в `node-pty@1.1.0/prebuilds/` лежат только `darwin-arm64` и `darwin-x64`,
    каталога `build/Release` нет вовсе — то есть нативная сборка node-pty на постинсталле не
    выполнялась. Тест утверждает, что `terminals.create()` сам чинит потерянный +x-бит у
    `spawn-helper` (баг распаковки pnpm) и PTY всё равно стартует. Это регресс «терминал не
    запускается», и сейчас он не проверяется ни на этом хосте, ни, вероятно, на Linux-CI.
    Действие: выяснить, почему не собирается `build/Release/spawn-helper` (пропущен postinstall
    node-gyp?), и либо починить сборку, либо обеспечить Linux-прибилд. Правка самого теста не
    нужна — он включится сам. Приёмка: `prebuilds`/`build` содержит linux-хелпер И файл теста
    перестал попадать в «skipped» полного прогона.

14. **`vela-login-activation-e2e.local.test.ts` не гоняется нигде.** Гейт `shouldRunRealVelaE2E()`
    требует одновременно `OD_RUN_REAL_VELA_LOGIN_E2E=1` и работающий бинарь `vela` в PATH
    (`execFileSync('vela', ['--version'])`); на хосте нет ни того, ни другого. Тест покрывает
    реальный поток логина: дочерний `vela login` печатает URL активации и код устройства,
    `readVelaLoginStatus()` их подхватывает, `cancelVelaLogin()` отменяет до подтверждения.
    Автоматизировать не следует — это живая сеть и ручной аппрув. Действие: завести ручной или
    nightly-прогон перед релизами vela-интеграции, чтобы поток не оставался непроверенным вовсе.

15. **`runtimes/launch.test.ts:20` (`winTest`) — дыра только при отсутствии Windows-раннера.**
    `const winTest = process.platform === 'win32' ? test : test.skip;` — обратная логика: тест
    идёт ТОЛЬКО на win32. Он проверяет, что `applyAgentLaunchEnv` добавляет каталог Node и
    wrapper-каталог в `Path`, когда демон запущен из GUI и нативного nodejs в PATH нет. Это
    реальный win32-путь кода. Действие: проверить, есть ли Windows в CI-матрице. Если нет —
    Windows-фикс PATH не проверяется автоматически никогда, и это настоящая дыра; если есть —
    пункт закрывается как «покрыто на другом раннере».

    ⚠️ Поправка к постановке, стоившая бы зря потраченного захода: из семи мест, помеченных
    инвентаризацией как `.skip`, реально выключены на этом хосте только три (13, 14, 15).
    Остальные четыре — `detection-diagnostics.test.ts:16`, `env-and-detection.test.ts:13`,
    `launch.test.ts:19`, `executables.test.ts:11` — это переменные-обёртки вида
    `win32 ? test.skip : test`, и на Linux они активны. Подтверждается независимо: полный прогон
    отчитался «3 skipped» файла, а не семь. Грепать `.skip` по коду недостаточно — надо смотреть
    значение условия на целевой платформе.

16. **`connection-test.test.ts`: испорченный proxy-env классифицируется как `auth_failed`, а не
    `unknown`.** Прогон в один поток, воспроизводится стабильно (не артефакт нагрузки хоста).
    `tests/connection-test.test.ts:2269` ждёт `{ ok: false, kind: 'unknown' }`, получает
    `{ ok: false, kind: 'auth_failed' }`. Значение видно пользователю: по `kind` UI решает, что
    показать — «проверьте ключ» вместо «проверьте прокси» отправляет чинить не то. Первый шаг —
    выяснить, чья это регрессия: прогнать этот файл на `upstream/main` в отдельном worktree.
    Совпало — дефект апстрима, заводить issue туда; разошлось — сломал наш мердж.

17. **`routine-schedule-claims.test.ts`: id разговора выигравшего запуска остаётся
    `routine-pending-conv-…`.** `expected 'routine-pending-conv-routine-run-919e…' to match
    /^routine-conv-/` — то есть после того как слот расписания забран, временный
    («pending») идентификатор не переименовывается в постоянный. Второй провал того же файла —
    `restores the reused project pin when the snapshot resolver throws mid-link`: статус остался
    `running` вместо `failed`, то есть упавший запуск не помечается упавшим и занимает слот.
    Второй симптом опаснее первого: расписание с застрявшим `running` перестаёт запускаться вовсе.
    В полном прогоне этот файл дал 4 провала, в одиночном 2 — часть завязана на параллель,
    но эти два от неё не зависят.

18. **`media/tasks-routes.test.ts`: маршрут отвечает 202 там, где ждут 400.**
    `marks the media task failed when proxy setup throws before generation starts` — при падении
    настройки прокси ДО начала генерации маршрут всё равно принимает задачу (202 Accepted) вместо
    отказа. Пользователь видит принятую задачу, которая никогда не выполнится. Как и 16-17,
    воспроизводится в один поток.

19. **`connectors-routes.test.ts` валится целиком по таймауту хука, а не по логике.**
    В полном прогоне 37 провалов из 55 — это ОДИН файл, и все они с `Error: Hook timed out in
    10000ms` в `tests/connectors-routes.test.ts:192`. То есть 37 «дефектов» — это один упавший
    `beforeEach` на хосте под нагрузкой. Действие: перепрогнать файл отдельно с
    `--hookTimeout=120000`. Позеленел — не дефект, а форма приёмки; и тогда пункт превращается в
    решение по пункту 20.

20. **[РАЗВИЛКА] Полный прогон демона на этом хосте надо гонять шардами, а не целиком.**
    Замер: 8717 тестов, 58 минут, `Duration 3465.89s` при load ~105 на 2 ядрах; из 55 провалов
    после изоляции остаётся 4 настоящих, остальные — таймауты (12× «Test timed out in 20000ms»,
    5× «Hook timed out in 10000ms»). Приёмка, которая на 93% состоит из шума среды, не приёмка.
    Выбрано: гонять по каталогам/группам файлов с поднятыми `--testTimeout`/`--hookTimeout`,
    а «весь набор одной командой» оставить для CI-раннера. Альтернатива (поднять таймауты
    глобально в конфиге) отклонена: она замаскирует настоящие зависания и на быстрой машине.

## C. Осталось только владельцу

- Решить, должен ли `open-design.cyberdub.su` быть публичным вообще: потребителей URL
  не найдено, а за ним — агент-демон на подписках владельца.
- Бэкап `~/.claude-od` и `~/.codex-od` (сейчас их не бэкапит ничто).
