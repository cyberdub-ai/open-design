# BACKLOG — форк open-design

Заведён 05.09.2026 автономным прогоном. Порядок = порядок работы: беру сверху.

Критерий попадания сюда: поведение, которое форк добавил или изменил поверх апстрима,
и которое ничем не удерживается. Апстримный мерж такое молча откатывает — ровно это
уже случилось дважды (`b93beada73` вернул `stdinOpen`, `e733c1118f` вернул `update_project`).

## A. Регрессии: поведение форка исчезло из дерева

1. **`OD_MCP_READONLY` / `WRITES_ENABLED` (из `e58fc262cf`) отсутствует полностью.**
   Все пишущие MCP-тулы регистрируются безусловно (`apps/daemon/src/mcp.ts`, `TOOL_DEFS`).
   Намерение оператора «только чтение» сейчас нечем выразить. Чинить с красным тестом:
   при `OD_MCP_READONLY=1` `localMcpToolDefinitions()` не содержит пишущих тулов.
2. ~~**`OD_EXTRA_ORIGINS`**~~ — ЗАКРЫТО 05.09, кода не требует. Форковый коммит `78cdef47d1`
   (а не `944fe8756e`, как значилось в инвентаризации) просто подмешивал строки в набор
   разрешённых origin. Апстримный `OD_ALLOWED_ORIGINS` делает то же строже: разбирает URL,
   отбивает не-http(s), нормализует origin (`apps/daemon/src/origin-validation.ts`). Это
   вытеснение, а не потеря, и прод на него уже переехал:
   `open-design.service` несёт `OD_ALLOWED_ORIGINS=https://open-design.cyberdub.su`.
   Возвращать форковое имя незачем — оно было бы вторым способом сказать то же самое.
3. ~~**`--skip-discovery` в CLI никогда не был реализован**~~ — ЗАКРЫТО 05.09, коммит
   `c763b2b93e`. Заявку сделал форковый коммит `78cdef47d1` (в инвентаризации стоял
   неверный `944fe8756e`), но `cli.ts` он не трогал вовсе. Реализовано: флаг зарегистрирован
   в `PROJECT_BOOLEAN_FLAGS` (без этого `parseFlags` отбивает его как неизвестный),
   описан в help и кладёт `skipDiscoveryBrief: true` в тело `POST /api/projects` только
   когда его попросили. Веб-поверхности нет намеренно — это то самое исключение из правила
   двойной поверхности: UI собирает бриф интерактивно, а флаг существует ровно для
   автоматических прогонов, где отвечать некому.
   Проверено так: `pnpm exec vitest run tests/cli-project-create-discovery.test.ts` → 3 passed
   (2 из 3 были красными до правки).

## B. Живое поведение форка без единого теста

4. ~~`stdinOpen=true`~~ — ЗАКРЫТО 05.09, коммит `7192bdeac4`. Утверждать константу
   `run.stdinOpen` было бы бесполезно: мерж, переразрешивший ветку, оставил бы тест зелёным.
   Поэтому обе ветки записи промпта вынесены в именованную
   `writeComposedPromptToChildStdin` (`apps/daemon/src/runtimes/chat-run-lifecycle.ts`),
   а тесты утверждают наблюдаемый эффект: какие байты ушли в stdin и был ли вызван `end()`.
   Поведение не менялось — JSONL-конверт, глотание EPIPE и учёт backpressure переехали как есть.
   Проверено так: `pnpm exec vitest run tests/chat-run-stdin-prompt.test.ts` → 7 passed
   (все 7 красные до появления хелпера).
5. ~~Тул `update_project`~~ — ЗАКРЫТО 05.09, коммит `bb103125b1`. Четыре случая на границе
   тула: в тело PATCH попадают только переданные поля; пустой патч отбивается без запроса;
   переименование сбрасывает 5-секундный кэш списка проектов; не-переименование его хранит.
   Проверено так: `pnpm exec vitest run tests/mcp-update-project.test.ts` → 4 passed; снятие
   строки `projectListCache = null` из `mcp.ts` красит ровно тест про переименование
   (мутационная проверка), после чего `mcp.ts` восстановлен байт-в-байт.
6. ~~Локальный Ollama по умолчанию~~ — ЗАКРЫТО 05.09, коммит `2493a2a779`. `OLLAMA_HOST`
   читается на загрузке модуля, поэтому тест грузит модули заново через `vi.resetModules()`
   и утверждает URL, по которому `extractWithLLM` реально ходит.
   Проверено так: `pnpm exec vitest run tests/memory-ollama-host.test.ts` → 2 passed.
7. ~~Сторож простоя codex 30 мин~~ — ЗАКРЫТО 05.09, коммит `51bad8e521`. Утверждается и поле
   `codexAgentDef.inactivityTimeoutMs`, и значение, которое отдаёт
   `resolveChatRunInactivityTimeoutMs` — тот же резолвер, что на боевом пути.
   Проверено так: `pnpm exec vitest run tests/runtimes/chat-run-inactivity-timeout.test.ts` → 32 passed.
8. ~~`OD_CLAUDE_STRICT_MCP`~~ — ЗАКРЫТО 05.09, коммит `2ddb2bb9dc`. Флаг появляется только при
   значении ровно `'1'`, отсутствует по умолчанию и при `'0'`/`'true'`/`'yes'`/`''`, и стоит
   раньше `--permission-mode`.
   Проверено так: `pnpm exec vitest run tests/runtimes/claude-strict-mcp-args.test.ts` → 4 passed.
9. ~~Выпадашка выбора скилла~~ — ЗАКРЫТО 05.09, коммит `192ce72d43`. Пять тестов на
   наблюдаемом контракте: какие опции даёт выпадашка на каждой вкладке, какой `skillId`
   доезжает до `onCreate` при «— авто —» и при явном выборе, что выбор не протекает между
   вкладками и что на вкладках без маршрутизации через скилл выпадашки нет.
   Проверено так: `pnpm --filter @open-design/web exec vitest run tests/components/NewProjectPanel.skill-picker.test.tsx`
   → 5 passed; мутационная проверка двумя правками ПО ОТДЕЛЬНОСТИ (снятие `selectedSkillId ??`
   из payload и снятие `useEffect` сброса на смене вкладки) — вместе они маскируют друг друга
   и красят только один тест; `pnpm --filter @open-design/web typecheck` → 0 `error TS`.

## B'. Найдено по ходу, не начато

10. ~~**[РАЗВИЛКА] Захардкоженный русский в UI.**~~ — ЗАКРЫТО 05.09, коммит `fb24d8bfeb`.
    Проверено так: восстановление литерала `Скилл` красит 4 теста из 5 (мутационная проверка),
    `pnpm --filter @open-design/web typecheck` → `WEB_TC=0`, 0 `error TS` (гейт на все 19 локалей).
    Исходная формулировка: `apps/web/src/components/NewProjectPanel.tsx:970`
    (`Скилл`) и `:978` (`— авто —`) — единственные нелокализованные строки интерфейса
    (полный скан кириллицы по `apps/web/src`: все остальные попадания — локализованные
    примеры промптов в `HomeHero.tsx`). Каждый неруссоязычный пользователь видит там кириллицу.
    Решение: отдельный коммит — ключи `newproj.skillLabel` / `newproj.skillAuto` в
    `i18n/types.ts` и во все 19 локалей, затем матчер в тесте с `/скилл/i` на английскую
    подпись. Скрипт правки готов: `scratchpad/i18n_skill_keys.py` (переводы для всех 19
    локалей вшиты, определяет стиль кавычек по файлу). Не начато сознательно: сессию
    закрывают, крупных задач больше не начинаю.

11. ~~**Локаль-условная ссылка на комьюнити не защищена тестом.**~~ — ЗАКРЫТО 05.09, коммит
    `71e208ed68`. Четыре спека в `apps/web/tests/components/EntryNavRail.community-locale.test.tsx`
    утверждают href, `data-testid` и `track()` для `zh-CN`, `zh-TW`, `en`, `ja`. Приёмка
    мутационная, по одной мутации за раз: снятие терма `|| locale === 'zh-TW'` → 1 failed |
    3 passed; схлопывание условия в `false` → 2 failed | 2 passed; восстановление →
    4 passed при пустом `git diff --stat` на компоненте.
    Исходная постановка: форк отдаёт китайским
    локалям инвайт в Feishu вместо Discord: `apps/web/src/components/EntryNavRail.tsx:1147`
    (`const isChinese = locale === 'zh-CN' || locale === 'zh-TW';`), используется на 1149/1168/
    1169/1171 — aria-label, `data-testid`, `track()`, иконка. Тестов нет вовсе: ни один файл в
    `apps/web/tests/**` не упоминает `Feishu`/`Discord`, включая `EntryNavRail.analytics.test.tsx`.
    Почему это опасно: поведение — один терм булева выражения внутри `EntryNavRail`, который
    апстрим активно правит (`#7237`, `#7202`, `#7190`). Мерж, переписавший соседний соцблок,
    разрешится без конфликта, если уберёт условие или схлопнет его в `false`; typecheck это
    пропустит, красить нечему. Тест должен утверждать наблюдаемое: при `locale='zh-CN'` и
    `'zh-TW'` в DOM ссылка на `feishu.cn` с `data-testid="entry-nav-rail-feishu"`, клик шлёт
    `track('feishu', …)`; при любой другой локали — `discord.gg` и `track('discord', …)`.
    Проверять именно ссылку, которую видит и кликает пользователь, а не наличие констант
    `DISCORD_URL`/`FEISHU_URL`. Приёмка — мутационная: убрать `|| locale === 'zh-TW'` и увидеть
    красный. Риск высокий, это единственная незащищённая форк-правка вне `apps/daemon`.

12. **Порядок карточки входа в футере nav rail не защищён (риск низкий).** Коммит `8934000924`
    заменил `position: absolute; bottom: 3px; z-index: 2` на `order: 2` для
    `.entry-local-mode-tip` в `apps/web/src/styles/home/entry-layout.css` (~421–520), чтобы
    непрозрачная карточка не перекрывала соцссылки (`order: 3`). CSS-мерж разрешается построчно
    и структурного якоря тут нет. Существующий `apps/web/tests/styles/cloud-signin-tip-selectable-text.test.ts`
    проверяет только выделяемость текста, не порядок. Тест должен утверждать эффект, а не
    значение свойства: у неавторизованного пользователя одновременно видимы и кликабельны и
    карточка входа, и соцссылки футера. Если это дорого выразить без реального лейаута — честнее
    завести как пункт для Playwright, чем писать проверку строки CSS.

13. **`terminals.spawn-helper.test.ts` не гоняется на Linux — молча.** Гейт:
    `const describeIfHelper = helperPath ? describe : describe.skip;`, где `helperPath` ищет
    `spawn-helper` в `<node-pty>/build/Release/` или `<node-pty>/prebuilds/<platform>-<arch>/`.
    На этом хосте в `node-pty@1.1.0/prebuilds/` лежат только `darwin-arm64` и `darwin-x64`,
    каталога `build/Release` нет вовсе — то есть нативная сборка node-pty на постинсталле не
    выполнялась. Тест утверждает, что `terminals.create()` сам чинит потерянный +x-бит у
    `spawn-helper` (баг распаковки pnpm) и PTY всё равно стартует. Это регресс «терминал не
    запускается», и сейчас он не проверяется ни на этом хосте, ни, вероятно, на Linux-CI.
    Действие: выяснить, почему не собирается `build/Release/spawn-helper` (пропущен postinstall
    node-gyp?), и либо починить сборку, либо обеспечить Linux-прибилд. Правка самого теста не
    нужна — он включится сам. Приёмка: `prebuilds`/`build` содержит linux-хелпер И файл теста
    перестал попадать в «skipped» полного прогона.

14. **`vela-login-activation-e2e.local.test.ts` не гоняется нигде.** Гейт `shouldRunRealVelaE2E()`
    требует одновременно `OD_RUN_REAL_VELA_LOGIN_E2E=1` и работающий бинарь `vela` в PATH
    (`execFileSync('vela', ['--version'])`); на хосте нет ни того, ни другого. Тест покрывает
    реальный поток логина: дочерний `vela login` печатает URL активации и код устройства,
    `readVelaLoginStatus()` их подхватывает, `cancelVelaLogin()` отменяет до подтверждения.
    Автоматизировать не следует — это живая сеть и ручной аппрув. Действие: завести ручной или
    nightly-прогон перед релизами vela-интеграции, чтобы поток не оставался непроверенным вовсе.

15. **`runtimes/launch.test.ts:20` (`winTest`) — дыра только при отсутствии Windows-раннера.**
    `const winTest = process.platform === 'win32' ? test : test.skip;` — обратная логика: тест
    идёт ТОЛЬКО на win32. Он проверяет, что `applyAgentLaunchEnv` добавляет каталог Node и
    wrapper-каталог в `Path`, когда демон запущен из GUI и нативного nodejs в PATH нет. Это
    реальный win32-путь кода. Действие: проверить, есть ли Windows в CI-матрице. Если нет —
    Windows-фикс PATH не проверяется автоматически никогда, и это настоящая дыра; если есть —
    пункт закрывается как «покрыто на другом раннере».

    ⚠️ Поправка к постановке, стоившая бы зря потраченного захода: из семи мест, помеченных
    инвентаризацией как `.skip`, реально выключены на этом хосте только три (13, 14, 15).
    Остальные четыре — `detection-diagnostics.test.ts:16`, `env-and-detection.test.ts:13`,
    `launch.test.ts:19`, `executables.test.ts:11` — это переменные-обёртки вида
    `win32 ? test.skip : test`, и на Linux они активны. Подтверждается независимо: полный прогон
    отчитался «3 skipped» файла, а не семь. Грепать `.skip` по коду недостаточно — надо смотреть
    значение условия на целевой платформе.

16. **`connection-test.test.ts`: испорченный proxy-env классифицируется как `auth_failed`, а не
    `unknown`.** Прогон в один поток, воспроизводится стабильно (не артефакт нагрузки хоста).
    `tests/connection-test.test.ts:2269` ждёт `{ ok: false, kind: 'unknown' }`, получает
    `{ ok: false, kind: 'auth_failed' }`. Значение видно пользователю: по `kind` UI решает, что
    показать — «проверьте ключ» вместо «проверьте прокси» отправляет чинить не то. Первый шаг —
    выяснить, чья это регрессия: прогнать этот файл на `upstream/main` в отдельном worktree.
    Совпало — дефект апстрима, заводить issue туда; разошлось — сломал наш мердж.

17. **`routine-schedule-claims.test.ts`: id разговора выигравшего запуска остаётся
    `routine-pending-conv-…`.** `expected 'routine-pending-conv-routine-run-919e…' to match
    /^routine-conv-/` — то есть после того как слот расписания забран, временный
    («pending») идентификатор не переименовывается в постоянный. Второй провал того же файла —
    `restores the reused project pin when the snapshot resolver throws mid-link`: статус остался
    `running` вместо `failed`, то есть упавший запуск не помечается упавшим и занимает слот.
    Второй симптом опаснее первого: расписание с застрявшим `running` перестаёт запускаться вовсе.
    В полном прогоне этот файл дал 4 провала, в одиночном 2 — часть завязана на параллель,
    но эти два от неё не зависят.

18. **`media/tasks-routes.test.ts`: маршрут отвечает 202 там, где ждут 400.**
    `marks the media task failed when proxy setup throws before generation starts` — при падении
    настройки прокси ДО начала генерации маршрут всё равно принимает задачу (202 Accepted) вместо
    отказа. Пользователь видит принятую задачу, которая никогда не выполнится. Как и 16-17,
    воспроизводится в один поток.

19. **`connectors-routes.test.ts` валится целиком по таймауту хука, а не по логике.**
    В полном прогоне 37 провалов из 55 — это ОДИН файл, и все они с `Error: Hook timed out in
    10000ms` в `tests/connectors-routes.test.ts:192`. То есть 37 «дефектов» — это один упавший
    `beforeEach` на хосте под нагрузкой. Действие: перепрогнать файл отдельно с
    `--hookTimeout=120000`. Позеленел — не дефект, а форма приёмки; и тогда пункт превращается в
    решение по пункту 20.

20. **[РАЗВИЛКА] Полный прогон демона на этом хосте надо гонять шардами, а не целиком.**
    Замер: 8717 тестов, 58 минут, `Duration 3465.89s` при load ~105 на 2 ядрах; из 55 провалов
    после изоляции остаётся 4 настоящих, остальные — таймауты (12× «Test timed out in 20000ms»,
    5× «Hook timed out in 10000ms»). Приёмка, которая на 93% состоит из шума среды, не приёмка.
    Выбрано: гонять по каталогам/группам файлов с поднятыми `--testTimeout`/`--hookTimeout`,
    а «весь набор одной командой» оставить для CI-раннера. Альтернатива (поднять таймауты
    глобально в конфиге) отклонена: она замаскирует настоящие зависания и на быстрой машине.

    Поправка 06.09 13:03 к причине, и она меняет вывод: load 105-369 на .44 — это НЕ упор в CPU,
    а проверка raid1-массива md0 (`/proc/mdstat`: check 49.6%, 23 процесса в состоянии D), то есть
    хост упёрт в ДИСК при почти свободном CPU. Горизонт «12 часов», стоявший здесь до 13:12, снят
    как ошибочный: поле `finish` у mdadm — мгновенная оценка от текущей скорости, а не прогноз, и
    за пять минут она изменилась с 708 до 33 минут (скорость выросла в двадцать раз, замер 13:05).
    Смотреть надо `/proc/mdstat` прямо сейчас, а не помнить чьё-то число. Прогон
    демона дисковый по своей природе — SQLite, `import 51.50s` из 250 с в изоляции, сотни фикстур.
    Значит «перепрогнать, когда нагрузка спадёт» — это отложить до конца проверки массива, а сами
    таймауты (12× «Test timed out in 20000ms») объясняются ожиданием ввода-вывода, а не зависанием
    кода. Шарды тем более правильны, но длительности прогонов сравнимы между собой только внутри
    одного состояния массива.

    Поправка 06.09 13:55 (agent-bus 20260906-135514), и она про сам выбранный план: шардирование
    вносит ровно ту дыру, о которой рассылка. Шард, чей glob не поймал ни одного файла, печатает
    ноль провалов НЕОТЛИЧИМО от чистого шарда, и молчит об этом сам vitest — пустая выборка не
    ошибка. Поэтому каждый шард обязан печатать число собранных файлов и тестов, а сумма по всем
    шардам сводиться с полным прогоном (8717 тестов; число файлов из того прогона не сохранено — снять
    заново при первом же шардированном заходе). Разошлось — часть набора не
    проверялась вовсе, и зелёный по шардам не значит ничего. Считать при этом надо РАЗЛИЧНЫЕ тесты,
    а не файлы: один файл даёт сотни тестов и создаёт видимость объёма.

21. **Замок паритета локалей видит только отсутствие ключа, а не непереведённое значение.**
    `apps/web/tests/i18n/locales.test.ts:342` и `:372` сравнивают `explicitLocaleKeys('zh-CN'|'ja')`
    с `Object.keys(en)` — то есть проверяют, что ключ объявлен, а не что значение отличается от
    английского. Ключ, объявленный английской строкой дословно, проходит замок молча и уезжает
    японскому читателю английским текстом. Значение вообще нигде не проверяется: в файле нет ни
    одной проверки по алфавиту (ни кириллица, ни CJK), а точечные проверки на :191 и :248 сам же
    файл на :325 называет «brittle per-key English lookups».

    Заведено по рассылке agent-bus 20260906-132406 (грабля флота: полуперевод невидим для сторожа).
    Прямая форма грабли к нам не относится — списка имён собственных на :401 стоит утверждением, а
    не предфильтром, снимающим ключи с проверки, и признака «есть не-латиница» у нас нет вовсе.
    Относится вторая половина: величина, которую проверяют по присутствию, а не по содержанию,
    выпадает из вопроса «переведено ли» целиком.

    Первый шаг — измерить, а не чинить: посчитать, у скольких ключей `zh-CN` и `ja` значение
    совпадает с `en` дословно. Ноль совпадений — замок достаточен, пункт закрывается замером.
    Ненулевое число даёт список настоящих дыр, и только тогда обсуждать сторож по содержанию
    (с явным списком исключений вроде `plugins.availableDetails.integrity`, который обязан
    оставаться английским). Приёмка мутационная: подменить одно японское значение на английское
    и убедиться, что новый сторож краснеет.

    Поправка 06.09 13:34 (agent-bus 20260906-133432), пункт она не отменяет, а уточняет две вещи.
    Первая: дырявым бывает признак по ОТСУТСТВИЮ признака цели («нет кириллицы — значит не
    переведено»), а признак по ПРИСУТСТВИЮ остатка исходника полуперевод ловит. У нас нет ни того
    ни другого, так что замок остаётся дырявым по своей причине — он не смотрит на значение вовсе.
    Замер из пункта поэтому обязан сравнивать значения ДОСЛОВНО: любой `trim`/`lower`/срез перед
    сравнением сам станет предфильтром и спрячет часть совпадений.
    Вторая: нормализация входа — молчаливый предфильтр. Проверено у нас и результат отрицательный:
    `explicitLocaleKeys()` (`locales.test.ts:33`) собирает ключи регуляркой по исходнику файла, то
    есть подаёт замку не словарь, а свою выжимку из текста — но выброшенный ею ключ делает наборы
    неравными и роняет тест КРАСНЫМ, а не пропускает молча. Этот предфильтр отказывает громко.

22. **Правило приёмки для пунктов 16-19: артефакт принимает, только если порождён правкой.**
    Заведено по рассылке agent-bus 20260906-134131. Каждый из пунктов 16-18 начинается со сверки
    с `upstream/main` в отдельном worktree, и это ровно та форма, где грабля срабатывает: зелёный
    прогон в worktree доказывает что-нибудь только если он ЗАПУЩЕН после чекаута этого worktree,
    а не переиспользован из соседнего дерева и не взят из `node_modules`/кэша vitest, собранных
    раньше. Перед принятием зелёного отвечать числами на два вопроса: сколько РАЗЛИЧНЫХ тестов в
    срезе (не файлов — файл даёт иллюзию объёма) и каков возраст прогона относительно правки.

    У нас уже есть форма, которая эту дыру закрывает по построению, и её надо держать: приёмка
    мутационная, по ОДНОЙ мутации за раз. Мутация обязана дать красный ДО правки — значит прогон
    гарантированно порождён изменением, а не унаследован. Пункт 11 закрыт именно так.
    Пункт 21 к этому же классу относится своей второй половиной: замер совпадений значений с `en`
    считать по РАЗЛИЧНЫМ ключам, а не по строкам файла.

23. **Флаги-заглушки: класс в коде ЕСТЬ, в живом процессе его НЕТ — одна правка от дыры.**
    Заведено по рассылкам agent-bus 20260906-171705 и поправке 20260906-172257 («искать ИМЯ
    флага, а не значение: ноль попаданий по значению не различает "флага нет" и "флаг=false"»).
    Проверка сделана ИМЕНЕМ и по живому процессу, а не по файлу секретов.

    Живой демон: `/usr/local/bin/node apps/daemon/dist/cli.js --port 7457 --host 0.0.0.0`,
    pid 3145293, контейнера нет вовсе — значит приёмка «docker inspect» из рассылки к нам не
    применима, эквивалент = `/proc/<pid>/environ`. В нём 38 переменных, все ключи `OD_*`:
    `OD_ALLOWED_ORIGINS`, `OD_API_TOKEN` (64 символа), `OD_CLAUDE_STRICT_MCP`, `OD_DAEMON_PORT`,
    `OD_SAFE_RUN_RETRY_MAX_ATTEMPTS`, `OD_WEB_PORT`. Ни одного имени из класса
    MOCK|DRY_RUN|SANDBOX|FAKE|STUB|BYPASS|SKIP_|DISABLE — в частности `OD_DISABLE_API_AUTH`
    отсутствует, а не стоит в false.

    Но класс в коде есть и он однострочный ровно как BILLING_MOCK_MODE у sellscope-os:
    `apps/daemon/src/api-token-auth.ts` → `isApiAuthDisabled()` возвращает
    `isTruthyEnvFlag(env.OD_DISABLE_API_AUTH)`, и `isApiTokenMiddlewareEnabled()` от него гаснет
    целиком. Собственные тесты демона это прибивают:
    `apps/daemon/tests/api-token-guard.test.ts:108` — стартует на публичном хосте БЕЗ токена,
    `:147` — снимает bearer-middleware даже когда `OD_API_TOKEN` задан. Демон публичен
    (`open-design.cyberdub.su`), то есть выставленный флаг был бы настоящей дырой, не спящей.

    `/srv/secrets/open-design.env` (163 Б) под моим uid не читается — но это свойство
    ИНСТРУМЕНТА, а не файла (рассылка 20260906-175150: «любая запись "проверить нечем" обязана
    называть, ЧЕМ именно нечем»). Инструмент, читающий то же самое снаружи, есть: `sudo -n cat`
    отдаёт файл целиком. Замер 06.09 (печатались только ИМЕНА, значения ни разу): ключ в файле
    ровно один — `OD_API_TOKEN`; попаданий класса MOCK|DRY_RUN|SANDBOX|FAKE|STUB|BYPASS|SKIP_|
    DISABLE по имени ПУСТО. То есть файл и живой процесс сходятся, каверна закрыта. [РАЗВИЛКА] Ставить сторож (тест, падающий при появлении
    `OD_DISABLE_API_AUTH` в проде) не берусь: прод-окружение демона за пределами репозитория,
    сторож в тестах его не увидит. Вместо этого — строка владельцу в разделе C.


24. **Живой демон исполняет сборку 23.08 — три коммита в `apps/daemon/src/` в неё не вошли**

    Правленый артефакт не есть исполняемое состояние (рассылка 20260906-175150). У Python это
    stale `__pycache__`, у нас — `dist/`: процесс pid 3145293 запущен из
    `apps/daemon/dist/cli.js`, собранного **2026-08-23 09:09** (606 файлов в `dist/`), а не из
    `src/`. Приёмка снимается с `dist/`, чтения `src/` не хватает.

    Что в исполняемой сборке ЕСТЬ (грепом по `dist/`): `OD_DISABLE_API_AUTH` в
    `dist/api-token-auth.js` и `dist/server.js`; `stdinOpen` ×2 и
    `applyClaudeStreamJsonRunBookkeeping` ×5 в `dist/server.js`; `update_project` ×2 в
    `dist/mcp.js` — то есть форковый фикс `e733c1118f` в бою.

    Чего в ней НЕТ. После сборки в `apps/daemon/src/{mcp,server}.ts` легли три коммита:
    `7192bdeac4` (тест на инвариант stream-json stdin), `687f833c85`
    (`feat(mcp): вернуть режим только-чтения OD_MCP_READONLY`), `448c98cc13` (схема
    `update_project` + инвариантный тест). Проверено грепом ИМЕНИ флага: `OD_MCP_READONLY`
    встречается в `src/mcp.ts` и ОТСУТСТВУЕТ в `dist/` целиком. Значит режим только-чтения MCP
    на публичном демоне сейчас недоступен вообще — не «выключен», а не существует. Всего новее
    сборки четыре файла: `src/server.ts`, `src/cli.ts`, `src/mcp.ts`,
    `src/runtimes/chat-run-lifecycle.ts` (последний правился 05.09 04:56).

    [РАЗВИЛКА] Пересборку и рестарт живого демона не делаю: прогон запрещает пересобирать прод
    вслепую, а рестарт рвёт активные раны. Это ход владельца — строка добавлена в раздел C.
    Дешёвый сторож, который тут уместен и репозиторию доступен: тест, сверяющий, что для каждого
    объявленного в `src/` env-имени найдётся вхождение в `dist/` — он ловит именно расхождение
    правленого и исполняемого, а не свежесть сборки по mtime.

25. **Два `od:`-сообщения слушаются, но их не посылает никто (ключ-продюсер vs ключ-потребитель)**

    Рассылка 20260906-181022: починенный сторож не доказывает, что в него что-то приходит; дефект
    живёт РОВНО В СТЫКЕ, оба конца по отдельности зелены, а греп по имени ключа даёт ноль
    совпадений — имена-то разные. У нас есть свой стык того же вида, и он даже хуже питоновского
    dict: `window.postMessage` между хостом `apps/web` и инжектируемым в iframe мостом — TypeScript
    на нём не помогает вообще, имена сообщений живут строками по обе стороны.

    Признак (AST-эквивалент — регулярки по ролям, не по вхождению имени): продюсеры —
    `type: 'od:…'` в литерале, `setItem('od:…')`, `new CustomEvent('od:…')`, хелперы
    `send('od:…')` / `postStroke('od:…')`; потребители — `=== 'od:…'`, `!== 'od:…'`,
    `getItem`, `addEventListener`. Тесты из выборки ИСКЛЮЧАЮТСЯ: иначе вход, поданный руками из
    теста, засчитывается за живого продюсера — ровно та ловушка, о которой рассылка и написана.
    Состав выборки печатать обязательно.

    СОСТАВ ВЫБОРКИ (06.09, только продакшн-код): файлов 1685, кладут 76 имён, читают 50.
    ЧИТАЮТ, НО НЕ КЛАДЁТ НИКТО — 2, обе проверены глазами построчно:

    - `od:palette` — мост слушает его в `apps/web/src/runtime/srcdoc.ts:1226` и по нему
      переписывает CSS-переменные палитры. Во всём продакшн-коде это сообщение не посылает никто:
      единственный отправитель — тест `apps/web/tests/runtime/srcdoc-palette-css-vars.test.ts:45`.
      То есть палитра-мост зелен в тестах и мёртв у пользователя.
    - `od:tweaks-panel-visible` — мост слушает на `srcdoc.ts:4182`, и собственный комментарий
      моста (`srcdoc.ts:4080`) прямо описывает: «host posts `od:tweaks-panel-visible` to drive
      panel visibility». Хост не посылает его нигде. Обратное направление живо
      (`od:tweaks-panel-state`, `od:tweaks-available` мост посылает), но в хосте нет ни одного
      слушателя и на них — то есть связка двусторонняя по документации и односторонне-мёртвая
      по обоим концам.

    Проверено, что это не артефакт признака: динамической сборки имён (`'od:' + x`,
    шаблонная подстановка) в `apps/web/src`, `packages`, `apps/daemon/src` нет ни одной; имена
    `od:palette` и `od:tweaks-panel-visible` встречаются в репозитории всего 2 раза каждое.
    Обратная сторона списка («кладут, но не читает никто» — 28 имён) в находки НЕ записана:
    это предсказуемый шум признака — ключи с динамическим суффиксом (`od:qs-recents:<id>`) и
    события, которые читаются через `addEventListener` по импортированной константе.

    [РАЗВИЛКА] Чинить не берусь в этой сессии: неизвестно, что задумано — вернуть отправителя
    или снять мёртвый мост, а это продуктовое решение. Приёмка правки, когда до неё дойдёт,
    по правилу рассылки: сторож смотрит на ВХОД, а не на результат — заглушка моста записывает
    полученное сообщение в список, RED до правки читается как «не пришло ничего».
    Само по себе это уже готовый инвариантный тест: «каждое `od:`-имя, которое кто-то слушает в
    продакшн-коде, имеет продакшн-отправителя» — он же ловит следующий такой разрыв автоматически.

26. **Журнал хоста живёт ~6 часов — доказательства прогонов сохранять своим файлом**

    Рассылка 20260906-181702, перемерено здесь независимо 06.09 18:17: `journalctl --disk-usage`
    → ровно 1.0G (потолок `SystemMaxUse`), первая запись журнала — 06.09 11:42:51 при живом
    аптайме 20 суток. Окно истории ВСЕГО журнала хоста — 6 ч 35 мин, не сутки.

    Прямое следствие для этого репозитория: полный прогон демона длится 58 минут и является
    прямой мишенью earlyoom (пункт про `exit 143`). Совет «сперва посмотри `journalctl -u
    earlyoom` за минуту смерти» работает, только если смотреть В ТОТ ЖЕ ДЕНЬ; через шесть часов
    записи стёрты, и причина смерти прогона становится непроверяемой навсегда. Поэтому длинные
    прогоны запускать с сохранением своего артефакта в момент замера: и `exit`-код, и хвост
    `journalctl -u earlyoom --since <старт прогона>` писать в файл в `docs/` или в скрэтчпад
    СРАЗУ, а не «потом посмотрим».

    Формулировка в журнал, общая: запись «отказов не было / в логах чисто» обязана называть ОКНО
    ИНСТРУМЕНТА. На этом хосте «за сутки в журнале ошибок нет» ложно по построению.
    ⛔ Три очевидных лечения закрыты и мной не трогаются: verbosity gost и увод gost в отдельный
    лог — это правка цепочки егресса (запрещена всем сессиям), `SystemMaxUse` в
    `/etc/systemd/journald.conf` — общесистемный root-конфиг владельца.

## C. Осталось только владельцу

- Решить, должен ли `open-design.cyberdub.su` быть публичным вообще: потребителей URL
  не найдено, а за ним — агент-демон на подписках владельца. Аутентификация сейчас РАБОТАЕТ
  (проверено 06.09 17:2x: публичный `/api/projects` без токена → 401, из LAN 192.168.2.44:7457
  → 401, подделка `X-Forwarded-For: 127.0.0.1` → 401, обход только с настоящего loopback-пира).
  Это снимает срочность, но не вопрос: единственная защита — один env-флаг `OD_DISABLE_API_AUTH`,
  которого сейчас в процессе нет (пункт 23).
- Не выставлять `OD_DISABLE_API_AUTH` в `/srv/secrets/open-design.env`: он выключает проверку
  токена целиком, а демон смотрит в интернет. Сейчас в файле его нет: `sudo -n cat` показал
  ровно один ключ, `OD_API_TOKEN` (проверено 06.09, значения не печатались).
- Пересобрать и перезапустить демон, когда это будет безопасно: живой процесс исполняет сборку
  23.08 09:09, в неё не вошли `7192bdeac4`, `687f833c85` (`OD_MCP_READONLY`), `448c98cc13`
  (пункт 23-бис/24). Рестарт рвёт активные раны, поэтому мой ход — только зафиксировать.
- Бэкап `~/.claude-od` и `~/.codex-od` (сейчас их не бэкапит ничто).
