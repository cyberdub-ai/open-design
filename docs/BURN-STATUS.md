# BURN-STATUS — open-design

Автономный прогон 05.09–07.09 2026. Формат: `HH:MM — что сделано — чем проверено`.

## 2026-09-05

- 02:10 — ротация утёкшего `OD_API_TOKEN` (последний пункт «осталось владельцу», который делался без человека) — гейт из шести проб: новый Bearer в LAN 200 / старый 401 / без токена 401 / аноним снаружи 401 / владелец Basic снаружи с новым 200, со старым 401. Коммит `7856eb3d6f`, откат — `open-design.service.bak-20260905`.
- 02:14 — вскрыта грабля прогона: shell по умолчанию даёт Node 20 через nvm, а репо требует `~24` (и `better-sqlite3` на 20 даёт тихий segfault) — `node --version` → v20.20.2 против `/usr/local/bin/node --version` → v24.13.0. Все гейты и сборки гоняю с `PATH=/usr/local/bin:$PATH`.
- 03:00 — `pnpm guard` зелёный после трёх независимых починок (каталог под `apps/` без манифеста — не приложение; дизайн-система считается скомпилированной только по обоим артефактам; `tools/import-refero-styles.py` внесён в границу `tools/`) — Проверено так: `pnpm guard` → `GUARD_EXIT=0`. Коммиты `eeacfeb722`, `635233f473`, `938d4918f5`; каждый с красным тестом до правки.
- 03:00 — [РАЗВИЛКА] осадок вынесенных апстримом воркспейсов (`apps/telemetry-worker`, `tools/pr`) — это неотслеживаемые `node_modules`-скорлупы, которые гарды принимали за содержимое репо. Удалять запрещено: `tools/pr` перемещён в `.tmp/extracted-leftovers/tools-pr.bak-20260905` (обратимо), остальное вылечено формулировкой инварианта в самих гардах, а не зачисткой дерева.
- 03:20 — форк-регрессия: тул `update_project`, возвращённый мержем 0.20.2, ехал без описаний пяти свойств схемы — ломал `pnpm typecheck` и был невидим вызывающему агенту — Проверено так: `pnpm --filter @open-design/daemon exec vitest run tests/mcp-write-tools.test.ts tests/mcp-brief-app.test.ts` → 2 files / 31 tests passed. Тест закрывает класс: описание обязано быть у каждого тула и каждого свойства. Коммит `448c98cc13`.
- 03:26 — `pnpm typecheck` зелёный на всём воркспейсе — Проверено так: `pnpm typecheck` (фон, PATH=/usr/local/bin) → exit 0.
- 03:40 — бэклог форка заведён (`docs/BACKLOG.md`): три исчезнувшие из дерева регрессии и шесть живых поведений форка без единого теста. Источник — полная инвентаризация 11 форк-коммитов.
- 03:40 — регрессия A1 закрыта: режим только-чтения `OD_MCP_READONLY=1` вернулся и стал строже прежнего — принадлежность к пишущим тулам берётся из аннотации `readOnlyHint`, а не из списка имён, и отказ стоит на диспетчере, а не только на `tools/list` — Проверено так: `pnpm exec vitest run tests/mcp-write-tools.test.ts` → 18 passed (4 новых теста были красными до правки); `pnpm --filter @open-design/daemon typecheck` → exit 0. Коммит `687f833c85`.
- 03:35 — [ФЛЕЙК, не чинил] `tests/mcp-spawn.test.ts` падает на закрытии сервера, когда весь набор `tests/mcp*` (26 файлов, 346 тестов) идёт параллельно, и проходит в одиночку — Проверено так: в наборе → 1 failed; `pnpm exec vitest run tests/mcp-spawn.test.ts` → 9 passed. Не моя правка: гонка портов в самом тесте.
- 04:05 — регрессия A3 закрыта: `od project create --skip-discovery` наконец существует — форк заявил флаг в сообщении коммита `78cdef47d1`, но `cli.ts` не тронул, и единственная аудитория флага (внешний агент, водящий OpenDesign через `od`) не могла до него дотянуться — Проверено так: `pnpm exec vitest run tests/cli-project-create-discovery.test.ts` → 3 passed (красные до правки); `pnpm guard` → exit 0; `pnpm --filter @open-design/daemon typecheck` → exit 0. Коммит `c763b2b93e`.
- 04:50 — бэклог B4 закрыт: инвариант «`stream-json` оставляет stdin ОТКРЫТЫМ» больше не держится комментарием. Обе ветки записи промпта вынесены в `writeComposedPromptToChildStdin`, тесты утверждают наблюдаемый эффект (какие байты ушли в stdin, был ли вызван `end()`), а не константу `run.stdinOpen` — константу мерж переписал бы, не покраснев — Проверено так: `pnpm exec vitest run tests/chat-run-stdin-prompt.test.ts` → 7 passed (все 7 красные до появления хелпера); `pnpm --filter @open-design/daemon typecheck` → exit 0. Коммит `7192bdeac4`.
- 04:52 — бэклог B5 закрыт: тул `update_project`, который мерж уже ронял однажды, получил тесты на поведение — только переданные поля в PATCH, отказ на пустом патче без запроса, сброс кэша списка проектов при переименовании и его сохранение без него — Проверено так: `pnpm exec vitest run tests/mcp-update-project.test.ts` → 4 passed; снятие `projectListCache = null` из `mcp.ts` красит ровно тест про переименование (мутационная проверка), `mcp.ts` восстановлен, `git diff --stat src/mcp.ts` пуст. Коммит `bb103125b1`.
- 04:53 — бэклог B6 закрыт: дефолт `OLLAMA_HOST` читается на загрузке модуля, и мерж, уронивший это чтение, оставил бы localhost, падающий только на боевом хосте — Проверено так: `pnpm exec vitest run tests/memory-ollama-host.test.ts` → 2 passed (URL, по которому реально ходит `extractWithLLM`). Коммит `2493a2a779`.
- 04:54 — бэклог B7 закрыт: 30-минутный сторож простоя codex (без него сторож убивает живой прогон как `stalled`, пока модель молча рассуждает) — Проверено так: `pnpm exec vitest run tests/runtimes/chat-run-inactivity-timeout.test.ts` → 32 passed. Коммит `51bad8e521`.
- 04:55 — бэклог B8 закрыт: ветка `OD_CLAUDE_STRICT_MCP` (живая в задеплоенном юните, покрытия не имела вовсе) — Проверено так: `pnpm exec vitest run tests/runtimes/claude-strict-mcp-args.test.ts` → 4 passed. Коммит `2ddb2bb9dc`.
- 05:30 — бэклог B9 закрыт (последний пункт секции B): выпадашка выбора скилла в мастере нового проекта — форк-поведение из одного терма `selectedSkillId ??` в payload и одного условного `<select>`, то есть ровно та форма, которую мерж разрешает без конфликта — Проверено так: `pnpm --filter @open-design/web exec vitest run tests/components/NewProjectPanel.skill-picker.test.tsx` → 5 passed; мутационная проверка двумя правками ПО ОТДЕЛЬНОСТИ (вместе они маскируют друг друга и красят только один тест из двух); `pnpm --filter @open-design/web typecheck` → 0 `error TS`. Коммит `192ce72d43`.
- 05:30 — [РАЗВИЛКА, не начато] в `NewProjectPanel.tsx:970,978` подпись «Скилл» и опция «— авто —» захардкожены по-русски и это единственные нелокализованные строки UI — Проверено так: полный скан кириллицы по `apps/web/src`, все прочие попадания — локализованные примеры промптов в `HomeHero.tsx`. Заведено пунктом 10 бэклога, скрипт правки на 19 локалей готов в скрэтчпаде. Сознательно не начато: сессия закрывается.
- 05:30 — [НЕ ПРОВЕРЕНО] полный набор тестов демона (`tests/**`) оборвался, не дойдя до конца: процесса нет, маркера `DAEMON_TESTS_EXIT` в логе нет, последний записанный файл — `tests/connectors-routes.test.ts`. Провалов в записанной части нет, но набор целиком НЕ подтверждён — перепрогнать в следующей сессии.
- 05:20 — бэклог 10 закрыт: подпись выпадашки скилла и её опция «— авто —» были захардкожены по-русски — единственные нелокализованные строки интерфейса, которые каждый неруссоязычный пользователь видел кириллицей. Ключи `newproj.skillLabel`/`newproj.skillAuto` заведены в типизированном `Dict` и во всех 19 локалях, поэтому пропущенный перевод теперь красит typecheck, а не молча падает в фолбэк — Проверено так: `pnpm --filter @open-design/web exec vitest run tests/components/NewProjectPanel.skill-picker.test.tsx` → 5 passed на АНГЛИЙСКОЙ подписи; возврат литерала `Скилл` красит 4 теста из 5 (мутационная проверка), после чего файл восстановлен и тесты снова 5 passed; `pnpm --filter @open-design/web typecheck` → `WEB_TC=0`, 0 `error TS`. Коммит `fb24d8bfeb`.
- 18:12 — принята директива владельца «инверсия маршрута делегирования до 06.09 12:00»: пул A (`Agent`, модель `sonnet`) впереди пула B, три субагента заняты постоянно, новых tmux-сессий не заводить; `delegate.sh` только для задач длиннее часа и `research-web`. Арифметика владельца: codex оплачен отдельной подпиской и Claude-лимит не тратит, поэтому сейчас работает мимо цели — сгорает именно недельный Claude-лимит (58%% израсходовано, ~18ч до сброса) — Проверено так: `~/.claude/CLAUDE.md:9` содержит блок «ВРЕМЕННАЯ ИНВЕРСИЯ МАРШРУТА — до 06.09.2026 12:00». Пункт (5) директивы оставляет пороги сжатия и закрытие долгих сессий без изменений, поэтому эта сессия (7 сжатий) закрывается штатно, а работа продолжается `/clear`-циклом в той же tmux-сессии.
- 18:12 — [БЛОКЕР ДЛЯ СЛЕДУЮЩЕЙ СЕССИИ] требование «держать три субагента занятыми» упирается в пустой бэклог: секция B закрыта целиком, пункт 10 закрыт, в секции C только работа владельца. Первым делом следующей сессии — перепрогнать полный набор тестов демона (прошлый прогон оборвался без итога) и завести новые пункты бэклога по его результату, иначе занимать три слота нечем.

- 18:34 — входящее «docker-лок свободен» разобрано: у open-design отложенных docker-операций нет, прогон целиком локальный (pnpm/vitest) — Проверено так: ab-ack 20260905-182931-monetezation-general-454b504e → ACK принят
- 18:50 — аудит форк-поведения вне apps/daemon завершён: заведены пункты 11 (локаль-условная ссылка Feishu/Discord в EntryNavRail — единственная незащищённая форк-правка, риск высокий) и 12 (порядок карточки входа в футере, риск низкий) — Проверено так: grep по apps/web/tests/** на Feishu|Discord → 0 вхождений, то есть тестов нет вовсе
- 19:06 — пункт 11 закрыт: тест на локаль-условную ссылку комьюнити (4 спеки, en/ja/zh-CN/zh-TW) — Проверено так: мутационная проверка по одной, снятие терма zh-TW красит 1 спеку, схлопывание условия в false красит 2, восстановление даёт 4 passed; git diff по компоненту пуст
- 19:06 — полный набор демона ПРОГНАН ДО КОНЦА впервые: 689 файлов, 8717 тестов, 3466 с — Проверено так: маркер DAEMON_TESTS_EXIT=1 в dts-full2.log, итог «8 failed | 678 passed | 3 skipped». Красные: 55 тестов, но 37 из них — один упавший по таймауту хук connectors-routes.test.ts:192, а не 37 дефектов; всего 17 падений класса «таймаут» при load 105 на двух ядрах
- 19:12 — разобраны выключенные тесты демона, заведены пункты 13-15 — Проверено так: полный прогон отчитался «3 skipped» файла, что независимо подтверждает вердикт: выключены три теста, а не семь; четыре оставшихся — обёртки win32 ? test.skip : test, на Linux активные. Грепать .skip по коду недостаточно, надо брать значение условия на целевой платформе
- 19:38 — изоляция трёх подозрительных файлов демона в один поток: 4 провала воспроизвелись, значит это настоящие дефекты, а не артефакты нагрузки — Проверено так: `vitest run --fileParallelism=false --maxWorkers=1 --testTimeout=120000 --hookTimeout=120000 tests/routine-schedule-claims.test.ts tests/media/tasks-routes.test.ts tests/connection-test.test.ts` → Test Files 3 failed (3), Tests 4 failed | 170 passed. Заведены пункты 16-20; 37 из 55 провалов полного прогона оказались одним файлом connectors-routes, упавшим по Hook timed out in 10000ms
- 13:05 — уточнена причина медленного прогона демона: хост упёрт в диск, а не в CPU — Проверено так: `/proc/mdstat` → md0 raid1 check 49.6%, finish=708 min, 23 процесса в состоянии D при почти свободном CPU (замер координатора 06.09 13:00). Пункт 20 бэклога дополнен: таймауты тестов — ожидание ввода-вывода, длительности прогонов сравнимы только внутри одного состояния массива
- 13:12 — снял из пункта 20 ошибочный горизонт «12 часов» — Проверено так: координатор опроверг сам себя замером 13:05:34 (`/proc/mdstat` → check 52.0%, finish=33.4min, speed 238811K/sec против 708min/11844K в 13:00). Поле finish у mdadm — мгновенная оценка от текущей скорости, а не прогноз; в силе остаётся только «упор в диск, не в CPU»
- 13:28 — завёл пункт 21: замок паритета локалей проверяет присутствие ключа, а не содержание значения — Проверено так: `sed -n 342,401p apps/web/tests/i18n/locales.test.ts` → сравнение `explicitLocaleKeys(...)` с `Object.keys(en)`, проверок значения нет; `grep -i -e cyrillic -e 0400-04FF -e allowlist` по файлу → пусто, то есть прямая форма рассылочной грабли (предфильтр + признак алфавита) у нас отсутствует
- 13:40 — уточнил пункт 21 по поправке о направлении признака и проверил у себя нормализацию входа — Проверено так: `explicitLocaleKeys()` (locales.test.ts:33) — регулярка по исходнику, но выброшенный ею ключ делает наборы неравными → тест краснеет, предфильтр отказывает громко; результат отрицательный с названной причиной
- 13:45 — завёл пункт 22: правило приёмки для сверок с upstream в worktree (артефакт принимает, только если порождён правкой) — Проверено так: применил к уже закрытому пункту 11 — там приёмка мутационная с красным ДО правки, то есть прогон порождён изменением по построению; дыра открыта только для будущих пунктов 16-19, где сверка идёт в отдельном дереве
- 14:02 — уточнил пункт 20: шардирование вносит дыру «пустой шард неотличим от чистого» — Проверено так: рассылка 20260906-135514 вопрос 3; требование записано — каждый шард печатает число собранных файлов/тестов, сумма сводится с полным прогоном (8717 тестов), иначе зелёный по шардам не значит ничего
- 14:05 — записал в хендофф защиту от earlyoom для прогонов демона — Проверено так: `journalctl -u earlyoom --since 13:30` в 17:01 → «low memory! at or below SIGTERM limits», swap 0 из 24575 МиБ, доступно 9.85% при пороге 10% — то есть угроза живая сейчас, а не вчерашняя; exit 143 у 58-минутного прогона проверять журналом earlyoom прежде кода
