# BURN-STATUS — open-design

Автономный прогон 05.09–07.09 2026. Формат: `HH:MM — что сделано — чем проверено`.

## 2026-09-05

- 02:10 — ротация утёкшего `OD_API_TOKEN` (последний пункт «осталось владельцу», который делался без человека) — гейт из шести проб: новый Bearer в LAN 200 / старый 401 / без токена 401 / аноним снаружи 401 / владелец Basic снаружи с новым 200, со старым 401. Коммит `7856eb3d6f`, откат — `open-design.service.bak-20260905`.
- 02:14 — вскрыта грабля прогона: shell по умолчанию даёт Node 20 через nvm, а репо требует `~24` (и `better-sqlite3` на 20 даёт тихий segfault) — `node --version` → v20.20.2 против `/usr/local/bin/node --version` → v24.13.0. Все гейты и сборки гоняю с `PATH=/usr/local/bin:$PATH`.
- 03:00 — `pnpm guard` зелёный после трёх независимых починок (каталог под `apps/` без манифеста — не приложение; дизайн-система считается скомпилированной только по обоим артефактам; `tools/import-refero-styles.py` внесён в границу `tools/`) — Проверено так: `pnpm guard` → `GUARD_EXIT=0`. Коммиты `eeacfeb722`, `635233f473`, `938d4918f5`; каждый с красным тестом до правки.
- 03:00 — [РАЗВИЛКА] осадок вынесенных апстримом воркспейсов (`apps/telemetry-worker`, `tools/pr`) — это неотслеживаемые `node_modules`-скорлупы, которые гарды принимали за содержимое репо. Удалять запрещено: `tools/pr` перемещён в `.tmp/extracted-leftovers/tools-pr.bak-20260905` (обратимо), остальное вылечено формулировкой инварианта в самих гардах, а не зачисткой дерева.
- 03:20 — форк-регрессия: тул `update_project`, возвращённый мержем 0.20.2, ехал без описаний пяти свойств схемы — ломал `pnpm typecheck` и был невидим вызывающему агенту — Проверено так: `pnpm --filter @open-design/daemon exec vitest run tests/mcp-write-tools.test.ts tests/mcp-brief-app.test.ts` → 2 files / 31 tests passed. Тест закрывает класс: описание обязано быть у каждого тула и каждого свойства. Коммит `448c98cc13`.
- 03:26 — `pnpm typecheck` зелёный на всём воркспейсе — Проверено так: `pnpm typecheck` (фон, PATH=/usr/local/bin) → exit 0.
- 03:40 — бэклог форка заведён (`docs/BACKLOG.md`): три исчезнувшие из дерева регрессии и шесть живых поведений форка без единого теста. Источник — полная инвентаризация 11 форк-коммитов.
- 03:40 — регрессия A1 закрыта: режим только-чтения `OD_MCP_READONLY=1` вернулся и стал строже прежнего — принадлежность к пишущим тулам берётся из аннотации `readOnlyHint`, а не из списка имён, и отказ стоит на диспетчере, а не только на `tools/list` — Проверено так: `pnpm exec vitest run tests/mcp-write-tools.test.ts` → 18 passed (4 новых теста были красными до правки); `pnpm --filter @open-design/daemon typecheck` → exit 0. Коммит `687f833c85`.
- 03:35 — [ФЛЕЙК, не чинил] `tests/mcp-spawn.test.ts` падает на закрытии сервера, когда весь набор `tests/mcp*` (26 файлов, 346 тестов) идёт параллельно, и проходит в одиночку — Проверено так: в наборе → 1 failed; `pnpm exec vitest run tests/mcp-spawn.test.ts` → 9 passed. Не моя правка: гонка портов в самом тесте.
